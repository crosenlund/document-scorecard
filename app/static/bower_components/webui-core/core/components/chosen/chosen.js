
// This component loads the jquery plugin from the
// jspm packages folder, and also includes our SPS
// specific style overrides.

// To build the CSS, run "gulp build-comp-css"

require('chosen/chosen.jquery.min');
require('./chosen.min.css!');
